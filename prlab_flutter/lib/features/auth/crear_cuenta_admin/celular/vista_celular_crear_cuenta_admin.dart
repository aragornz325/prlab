import 'package:flutter/material.dart';

/// {@template VistaCelularCrearCuentaAdmin}
/// vista de celular que dependiendo el tamaño de la dispositivo varia
/// entre celular y escritorio
/// {@endtemplate}
class VistaCelularCrearCuentaAdmin extends StatelessWidget {
  /// {@macro VistaCelularCrearCuentaAdmin}
  const VistaCelularCrearCuentaAdmin({super.key});

  // TODO(Mati): Hablar con guille de que agregue esta pagina para celular.
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
