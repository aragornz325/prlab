import 'package:flutter/material.dart';

/// {@template VistaCelularInicio}
/// Vista de celular del Inicio del dashboard, seccion que el usuario ve
/// cuando inicia sesión
/// {@endtemplate}
class VistaCelularInicio extends StatelessWidget {
  /// {@macro VistaCelularInicio}
  const VistaCelularInicio({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(Gon): Hacer la vista de celular de Inicio cuando haya diseño
    return const Placeholder();
  }
}
