import 'package:auto_route/annotations.dart';
import 'package:flutter/material.dart';

// TODO(anyone): Agregar documentación.
@RoutePage(name: 'RutaSelectorDeDestinatarios')
class SelectorDeDestinatarios extends StatelessWidget {
  const SelectorDeDestinatarios({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(anyone): Crear los componentes para esta parte de la feature.
    return const Center(
      child: Text('Selector de destinatarios'),
    );
  }
}
